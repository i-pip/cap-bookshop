{
  "name": "bookshop",
  "version": "1.0.0",
  "description": "Generated by cds init",
  "repository": "<Add your repository here>",
  "license": "ISC",
  "dependencies": {
    "@sap/cds": "^3.18.3",
    "express": "^4.17.1"
  },
  "engines": {
    "node": "^8.9"
  },
  "scripts": {
    "build": "cds build/all --clean",
    "deploy": "cds deploy",
    "start": "cds run"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "model": [
          "db",
          "srv",
          "app",
          "./schema.cds",
          "./services.cds"
        ],
        "credentials": {
          "database": "my.db"
        }
      }
    }
  }
}
